str = prompt("Введите строку");

let max = str.slice(0, 20);
let temp = "...";

function truncate(str) {
    if (str.length > max.length) {

    console.log(str)
    return result = max + temp;
}

}

alert(truncate(str));
