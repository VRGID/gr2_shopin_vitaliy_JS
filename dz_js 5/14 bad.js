let schedule = prompt();

let time = "8:30"

function is_empty(obj) {
  for (typeof schedule == typeof time) {
    alert(is_empty(schedule))
    return false;
  }
    let schedule = ("подъём");
  alert(is_empty(schedule))
  return true;
}